---
title: 数组与链表
date: 2020-04-14 13:49:49
categories: 
- 数据结构
tags:
- 数组
- 链表
---

# 数组与链表

数组与链表都是线性表的一种

## 数组

真数组是在内存中连续的，是一种顺序的存储结构。

### 优点

- Java实现了真数组，数组在内存中连续分配，可以通过下标直接访问目标位置的元素
- Java数组所在内存为数组专用，不会出现数据覆盖的问题

### 缺点

- 插入与删除效率低
- 需要预先分配内存空间，可能出现内存空间的浪费
- 数组大小是固定的，无法动态扩展，若扩充只能开辟新的数组将原来的数组复制到另一个内存空间更大的数组中

## 链表

链表是通过引用连接元素的链式存储结构。

### 优点

- 链表的插入与删除只需知道引用便可以在O(1)的时间复杂度内完成
- 链表无需预先分配内存，没有内存空间的浪费
- 链表不固定大小，扩展链表很容易

### 缺点

- 链表无法通过下标访问数据，查找是必须从head开始遍历